<el-table-column label="{COLUMN_COMMENT}" prop="{COLUMN_NAME}" sortable>
    <template #default="{ row }">
        <el-image style="width: 50px; height: 50px" :src="row.{COLUMN_NAME}" />
    </template>
</el-table-column>